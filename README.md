# VDS_Variational_Decision_Shield
A domain-agnostic decision policy that minimises a composite variational functional J(π) = L + αU + βR + γC − δI across selective classification and cyber-physical energy management. Multi-seed validation with worst-case attacks.
